import RN from 'react-native'

export const styles = RN.StyleSheet.create({

    container: {
        paddingHorizontal: 16,
        height: '100%'
    },
    flatList: {
        flex: 1,
        backgroundColor: '#fff',
        paddingTop: 24
    },
    titleText: {
        fontSize: 25,
        fontWeight: '600',
        textAlign: 'center',
        marginBottom: 16
    }

})